service: sentiment
frameworkVersion: '2'

provider:
  name: aws
  runtime: python3.8
  iam:
    role:
      name: sentiment-analysis-role
      statements:
        - Effect: 'Allow'
          Resource: '*'
          Action: 'comprehend:*'

plugins:
  - serverless-python-requirements

functions:
  hello:
    handler: handler.hello
    events:
      - httpApi:
          path: /
          method: post
